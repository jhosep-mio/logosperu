const id: string = ''
const name: string = ''
const email: string = ''
// eslint-disable-next-line @typescript-eslint/naming-convention
const email_alter: string = ''
const firma: string = ''
// eslint-disable-next-line @typescript-eslint/naming-convention
const id_rol: number = 0
// eslint-disable-next-line @typescript-eslint/naming-convention
const pass_email: string = ''

export const UserSchema = {
  id,
  name,
  email,
  email_alter,
  firma,
  id_rol,
  pass_email
}
